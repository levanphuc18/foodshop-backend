spring.application.name=foodshop

#datasource
spring.datasource.url=${mysql_url}
spring.datasource.username=${mysql_username}
spring.datasource.password=${mysql_password}

# Hibernate JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT config
jwt.secret=${jwt_secret}
jwt.access.expiration-ms=${jwt_access_expiration}   # 10 gio
jwt.refresh.expiration-ms=${jwt_refresh_expiration}   # 100 gio


## Flyway
#flyway:
#enabled: true
#locations: classpath:db/migration
#baseline-on-migrate: true           # Cho phep chay khi db da co du lieu
